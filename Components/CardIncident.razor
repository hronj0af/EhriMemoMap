@using EhriMemoMap.Data;
@using EhriMemoMap.Helpers;
@using System.Xml;
@using System.Xml.Linq;
@inject IStringLocalizer<CommonResources> cl

<RadzenRow class="rz-mb-4">
    <RadzenCard Style="width:100%">

            <ul class="no-bullets">
                <li><b>@(CultureInfo.CurrentCulture.Name == "en-US" ? Place.LabelEn : Place.LabelCs)</b></li>
@*                 <li><i>@WMSFeatureInfo.Date?.ToString("d. M. yyyy")</i></li>
                <li>@cl["place"]: @WMSFeatureInfo.Place</li>
                @if (WMSFeatureInfo.Description != "NULL")
                {
                    <li>@cl["description"]: @WMSFeatureInfo.Description</li>
                }
                <li>@cl["type"]: @WMSFeatureInfo.Type</li>
                @if (WMSFeatureInfo.Specification != "NULL")
                {
                    <li>@cl["specification"]: @WMSFeatureInfo.Specification</li>
                }
 *@                <li>@cl["documents"]: @((MarkupString)(CultureInfo.CurrentCulture.Name == "en-US" ? Place.DocumentsEn : Place.DocumentsCs)?.Linkify())</li>
            </ul>

    </RadzenCard>
</RadzenRow>

@code {
    [Parameter]
    public PragueIncidentsTimeline Place { get; set; }
}
