@inject MapService MapService

@if (MapService.MapState == MapStateEnum.Rendered && MapService.IsMobileView == false)
{
    <LayersQuickAccessButtons />
}

@* @if (MapService.MapType != MapTypeEnum.Normal)
{
    <StoryMapButtons />
}
 *@
@code {

    protected override void OnInitialized()
    {
        MapService.OnChange += StateHasChanged;
    }

    public void Dispose()
    {
        MapService.OnChange -= StateHasChanged;
    }


}
